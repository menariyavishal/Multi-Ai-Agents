# Web Framework & Server
flask>=3.0.0                          # Lightweight web framework for REST API endpoints
gunicorn>=21.0.0                      # Production WSGI HTTP server for deploying Flask app

# Configuration & Environment
python-dotenv>=1.0.0                  # Load environment variables from .env file
cryptography>=40.0.0                  # Encryption/hashing for API keys and sensitive data

# Data Validation & Serialization
pydantic>=2.0.0                       # Type-safe request/response validation using Python type hints

# AI Orchestration & LangGraph
langchain>=0.2.0                      # LLM orchestration, tool calling, and chain management
langgraph>=0.1.0                      # State machine for multi-agent workflow (core to our 5-agent pipeline)
langchain-google-genai>=0.0.5         # Integration with Google Gemini API for Planner and Writer agents
langchain-community>=0.0.10           # Community LLM integrations (HuggingFace, Mistral, Llama, Zephyr)
langchain-huggingface>=0.0.1          # HuggingFace integrations for Endpoint API
huggingface_hub>=0.16.0               # HuggingFace Hub API client for model inference

# Databases & Caching
pymongo>=4.0.0                        # MongoDB driver for storing chat history and graph state snapshots
diskcache>=5.0.0                      # Disk-based caching for query results and LLM responses

# HTTP & Utilities
requests>=2.28.0                      # HTTP library for MCP tools (web search, API calls)
